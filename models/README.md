# Keep this folder — model weights are downloaded separately and ignored by git.
# See README.md for download links.
#
# Required files:
#   shape_predictor_68_face_landmarks.dat   — dlib 68-point ERT model
#   face_detection_yunet_2023mar.onnx       — OpenCV YuNet face detector (fallback)
#
# Optional:
#   mmod_human_face_detector.dat            — dlib CNN face detector (alternate)
